pydantic>=2.0,<3
sqlalchemy>=2.0,<3
alembic>=1.13
aiosqlite>=0.19        # async SQLite driver (dev / testing)
asyncpg>=0.29          # async PostgreSQL driver (production)
greenlet>=3.0          # required by SQLAlchemy async extension
psycopg2-binary>=2.9   # sync PostgreSQL driver (used by Alembic migrations)